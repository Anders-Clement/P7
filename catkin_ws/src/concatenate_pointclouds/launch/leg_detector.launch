
<launch>  
  <node pkg="leg_detector" type="leg_detector" name="leg_detector"
        args="$(find concatenate_pointclouds)/config/trained_leg_detector.yaml" output="screen">
        
        <param name="fixed_frame" type="string" value="pge" />

  </node>
</launch>
